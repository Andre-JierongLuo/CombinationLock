Implement a combination lock that has an n-number combination.
Using the Model-View-Controller design pattern.

2014-5-26:
simply create the lock with an initial password 1-2-3.
Can't change that password for now.
Maybe add a function that reset the password later.
